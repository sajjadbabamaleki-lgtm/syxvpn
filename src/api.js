const BASE=import.meta.env.VITE_API_URL||'http://localhost:8787';async function request(path,options){const r=await fetch(`${BASE}${path}`,{headers:{'Content-Type':'application/json'},...options});if(!r.ok)throw new Error(`API ${r.status}`);return r.json()}export const api={overview:()=>request('/api/overview'),gateways:()=>request('/api/gateways'),subscribers:()=>request('/api/subscribers'),routes:()=>request('/api/routes'),createSubscriber:data=>request('/api/subscribers',{method:'POST',body:JSON.stringify(data)}),checkGateway:id=>request(`/api/gateways/${id}/check`,{method:'POST'})};