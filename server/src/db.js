import Database from 'better-sqlite3';
const db=new Database(process.env.DB_PATH||'jordan.db');db.pragma('journal_mode = WAL');
db.exec(`CREATE TABLE IF NOT EXISTS subscribers(id TEXT PRIMARY KEY,name TEXT NOT NULL,token TEXT UNIQUE NOT NULL,quota_bytes INTEGER NOT NULL DEFAULT 10737418240,used_bytes INTEGER NOT NULL DEFAULT 0,expires_at TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);CREATE TABLE IF NOT EXISTS gateways(id TEXT PRIMARY KEY,name TEXT NOT NULL,region TEXT NOT NULL,kind TEXT NOT NULL CHECK(kind IN ('gateway','egress')),host TEXT NOT NULL,port INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'unknown',latency_ms INTEGER,throughput_mbps INTEGER DEFAULT 0,last_check TEXT,priority INTEGER NOT NULL DEFAULT 100);CREATE TABLE IF NOT EXISTS health_events(id INTEGER PRIMARY KEY AUTOINCREMENT,gateway_id TEXT NOT NULL,status TEXT NOT NULL,latency_ms INTEGER,checked_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(gateway_id) REFERENCES gateways(id));`);
export default db;